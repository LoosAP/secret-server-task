{
  "name": "secret-server-task",
  "version": "1.0.0",
  "description": "Your task is to implement a secret server. The secret server can be used to store and share secrets\r using the random generated URL. But the secret can be read only a limited number of times after that\r it will expire and won’t be available. The secret may have TTL. After the expiration time the secret\r won’t be available anymore. You can find the detailed API documentation in the swagger.yaml file.\r We recommend to use [Swagger](https://editor.swagger.io/) or any other OpenAPI implementation to\r read the documentation.",
  "main": "index.js",
  "scripts": {
    "start": "ts-node src/server.ts",
    "build": "tsc",
    "serve": "node dist/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^5.0.0",
    "@types/mongoose": "^5.11.97",
    "@types/node": "^22.13.9",
    "@types/xml2js": "^0.4.14",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "mongoose": "^8.12.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.2",
    "uuid": "^11.1.0",
    "xml2js": "^0.6.2"
  }
}
